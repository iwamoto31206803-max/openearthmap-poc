python -c "from pathlib import Path; from PIL import Image; root=Path(r'C:\OpenEarthMap_PoC\data\gsi\raw\water_572'); org=root/'org'; val=root/'val'; mism=[]; n=0; import os; 
for p in sorted(org.glob('*.png')):
    q=val/p.name
    if not q.exists():
        print('MISSING VAL', p.name)
        continue
    so=Image.open(p).size; sv=Image.open(q).size; n+=1
    if so!=sv: mism.append((p.name,so,sv))
print('pairs checked =',n)
print('size mismatches =',len(mism))
print('first mismatches =',mism[:50])"
